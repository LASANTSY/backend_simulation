// Auth routes removed per project requirements. This file remains as a harmless stub
// to avoid breaking imports in places that may still reference it.
import express from 'express';

const router = express.Router();

// No routes exported.

export default router;
